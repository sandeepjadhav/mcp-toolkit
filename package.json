{
  "name": "mcp-toolkit",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/mongodb"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}